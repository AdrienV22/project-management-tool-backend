# URL pour la base de données de test
spring.datasource.url=jdbc:postgresql://localhost:5432/project_management_test
spring.datasource.username=postgres
spring.datasource.password=2292951665Tokyo

spring.profiles.active=test

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.datasource.driver-class-name=org.postgresql.Driver

# Spécifie le dialecte PostgreSQL pour Hibernate (base de données de test)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuration de JPA/Hibernate pour les tests
spring.jpa.properties.hibernate.format_sql=true
